% PLANTILLA de aporte a la galería.
% Copia esta carpeta (_template) a gallery/<categoría>/<autor>-<nombre>/
% y reemplaza el contenido. example.tex debe COMPILAR solo.
%
% Opción A — usar el design system del kit:
%   \input{design/preamble.tex}  \setsubject{mat}  ...  \begin{document} ...
% Opción B — preámbulo propio (autocontenido), para que sirva fuera del kit.

\documentclass[11pt]{article}
\usepackage{fontspec}
\usepackage{polyglossia}\setdefaultlanguage{spanish}
\begin{document}

\section*{<Tu diseño aquí>}
Reemplaza esto por tu ejemplo corto. Mantenlo simple y representativo del estilo.

\end{document}
